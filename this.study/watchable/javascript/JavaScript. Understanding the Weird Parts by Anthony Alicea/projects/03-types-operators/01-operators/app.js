// The addition operator. Written in "infix" notation, as opposed to
// prefix or  postfix notations.
var a = 3 + 4;

var b = 4 - 3;

var c = 4 > 3;

console.log(a, b, c);

// These operators are special types of functions that the operands are being
// passed as arguments and a value is being returned. Inside those functions,
// there's pre-built logic written for you that the JavaScript engine provides.